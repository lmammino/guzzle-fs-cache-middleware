{
    "name": "lmammino/guzzle-fs-cache-middleware",
    "description": "A minimalist and opinionated Cache Middleware for Guzzle that uses the FileSystem as backend",
    "keywords": [
        "http",
        "cache",
        "filesystem",
        "guzzle",
        "client",
        "HTTP client"
    ],
    "license": "MIT",
    "type": "library",
    "require": {
        "php": ">=5.6",
        "symfony/cache": "~3.4"
    },
    "require-dev": {
        "guzzlehttp/guzzle": "^6.5"
    },
    "suggest": {
        "guzzlehttp/guzzle": "The HTTP client library that guzzle-fs-cache-middleware can be used with"
    },
    "autoload": {
        "psr-4": {
            "LM\\": "src/"
        }
    }
}
